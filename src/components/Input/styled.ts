import styled from "styled-components";
import Input from ".";

export const StyledInput = styled(Input)`
  margin-top: 50px;
  border: none;
  outline: none;
  font-family: sans-serif;
  font-size: 18px;
`;
